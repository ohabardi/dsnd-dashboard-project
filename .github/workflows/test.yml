name: Test Trigger

on:
  push:
    branches:
      - main

jobs:
  test:
    runs-on: ubuntu-latest
    steps:
      - name: Print Hello
        run: echo "✅ GitHub Actions Triggered Successfully!"
